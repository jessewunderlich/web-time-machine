# Holistic Audit — 2026-05-07

Repo: `jessewunderlich/web-time-machine` · Live: web-time-machine-coral.vercel.app
Scope: full-stack pass (code, deps, security, content, SEO, hygiene).

## Snapshot

| Check | Result |
|---|---|
| `tsc --noEmit` | ✅ clean |
| `eslint --max-warnings 0` | ✅ clean |
| `next build` | ✅ 22 routes, static generation OK |
| `npm audit` | ⚠️ 2 moderate (postcss, transitive via Next 16) |
| Open PRs | 0 |
| Stale remote branches | 10 |
| Source LOC | ~5,400 (TS/TSX) |

## Fixed in this PR

1. **Era 2021 panel title regression.** Last merge resolved a conflict against the wrong winner — title read `(2021–2026)` while every other surface (EraNav, EraContainer, JsonLd) correctly labels the era `2021–2025` and treats 2026 as a separate "Agentic Era." Restored to `(2021–2025)`.
2. **Centralized site URL** in `src/lib/site.ts`. Six files were each hardcoding `web-time-machine-coral.vercel.app` plus reinventing the `NEXT_PUBLIC_SITE_URL` / `VERCEL_URL` / `VERCEL_ENV` fallback ladder. Now a single import. Migrating to a custom domain is one env var change.
3. **Archived 3 historical roadmaps** to `docs/archive/roadmaps/`. Repo root no longer carries `ROADMAP.md`, `ROADMAP-2026-04-18.md`, `ROADMAP-2026-04-18-v3.md`.
4. **Dep bumps**: `next` 16.2.4 → 16.2.6, `react`/`react-dom` 19.2.5 → 19.2.6, `eslint-config-next` 16.2.6.

## Known and intentionally deferred

- **postcss moderate CVE (GHSA-qx2v-qp2m-jg93).** Pinned transitively by `next` across the entire 16.x line up to 16.3.0-canary. `npm audit fix --force` would downgrade to `next@9.3.3` (breaking). Resolution = wait for Next 16.3 stable.
- **No CSP.** Inline scripts/styles from Next + GSAP + Vercel Analytics need nonces; deferred. Baseline security headers are in place (`X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy`).
- ~~**GSAP imported in 9 era components.**~~ Shipped in PR #15 (`src/lib/gsap.ts` central entry point).
- ~~**No Lighthouse CI.**~~ Shipped: `.github/workflows/lighthouse.yml` audits Vercel preview on PRs and production on push to main. Budgets in `.lighthouserc.json`.
- **No Playwright visual regression / unit tests.** Roadmap D.2 still open.
- **`feed.atom` entries dated 2026-04-18.** Real-world sense says they're stale relative to the current main (per-era OG, embeds, view transitions are all live now). Either refresh on each meaningful ship, or remove the feed if it's not maintained.
- **`arc23.webp` is 349 KB**, ~2× the next largest screenshot. Re-encode at lower quality.
- **10 stale remote branches** from prior audit rounds (`fix/audit-round-2..6`, `chore/fresh-audit-fixes`, etc.) — recommend cleanup separately.

## Strengths

- TS strict mode; ESLint clean with `--max-warnings 0`.
- Solid security-headers baseline in `next.config.ts`.
- Per-era OG + Twitter cards, sitemap, robots, JSON-LD, Atom feed — full SEO surface.
- A11y scaffolding is real, not cosmetic (`HelpModal` has focus management, focus restore, Esc-to-close, click-outside, `aria-modal`).
- `AGENTS.md` captures hard-won invariants ("never `dynamic(..., { ssr: false })` on Era components" / "use `'use client'` for GSAP/IntersectionObserver/Audio").
- Vercel Analytics + Speed Insights wired for real-user data.
